# backend-empresa

## Credenciales locales (no versionadas)

El archivo local `test` no se versiona y esta en `.gitignore`.
Puedes copiar el ejemplo para crear tu archivo local:

```bash
cp test.sample.json test
```